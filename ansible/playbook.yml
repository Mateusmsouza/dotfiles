- name: Executar comando simples
  hosts: local
  gather_facts: yes

  tasks:
    - name: Rodar uptime
      command: uptime
      register: resultado

    - name: Mostrar saída
      debug:
        var: resultado.stdout

  roles:
    - git